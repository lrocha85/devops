pipeline {
    agent {
        label 'master'
    }

    stages{
        stage('Clone repository') {
            echo "clonando"
        }

        stage('Echo') {
            echo "Repositório clonado"
        }
    }

}
